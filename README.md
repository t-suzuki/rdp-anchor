# RDP Launcher

**再起動でモニターIDがずれても、意図通りのマルチモニターRDP接続ができるGUIランチャー。**

## 何を解決するか

`mstsc /l` が返すモニターID (0, 1, 2...) は再起動やドック着脱で変わる。
`.rdp` ファイルの `selectedmonitors:s:0,1` はIDベースなので、ずれると意図しないモニターに接続してしまう。

**核心**: モニターIDは不安定だが、**画面の物理座標と解像度は不変**。
本ツールは座標ベースでモニターを識別し、接続時に正しいIDへ自動変換する。

## 機能

- **複数ホスト管理**: .rdp ファイル単位でホストを登録、カード型UIで一覧
- **ディスプレイプロファイル**: 「左2画面」「右2画面」「全画面」など名前付きで定義
- **ホスト × プロファイル**: ホストごとにデフォルトプロファイルを設定、接続時に切替も可
- **接続ガード**: 既に接続中のホストに再接続しようとすると確認ダイアログを表示
- **元の.rdpを温存**: モニター設定だけ一時コピーに書き換えてmstscに渡す。他の設定はそのまま
- **ゼロ依存**: シングルバイナリ (WebView2はWin10/11標準搭載)
- **高速起動**: Tauri + Rust、ネイティブ並みの起動速度

## アーキテクチャ

```
┌─────────────────────────────────────────────────┐
│  Frontend (HTML/CSS/JS in WebView2)             │
│  ・ホストカード一覧                               │
│  ・モニター視覚プレビュー                          │
│  ・プロファイル選択                                │
│  ・設定画面 (モニター / プロファイル管理)            │
└──────────────────┬──────────────────────────────┘
                   │ Tauri invoke (IPC)
┌──────────────────▼──────────────────────────────┐
│  Backend (Rust)                                  │
│                                                  │
│  monitor.rs  mstsc /l パース + Win32 API         │
│              座標ベースでID逆引き                   │
│                                                  │
│  rdp.rs      .rdp ファイル読み書き                 │
│              selectedmonitors だけ動的書き換え      │
│                                                  │
│  session.rs  EnumWindows で接続中セッション検出      │
│                                                  │
│  config.rs   JSON 設定の永続化                     │
│              %APPDATA%/rdp-launcher/config.json   │
└─────────────────────────────────────────────────┘
```

## 接続フロー

```
[接続ボタン]
    │
    ▼
mstsc /l を実行 → 現在のID⇔座標マップ取得
    │
    ▼
プロファイルの各モニター定義を座標で照合 → 現在のmstsc IDを特定
    │
    ▼
元の .rdp をコピー → selectedmonitors だけ書き換え
    │
    ▼
既に接続中？ → Yes → 確認ダイアログ → No → 中止
    │                      │
    ▼                      ▼
mstsc.exe <temp>.rdp で起動
```

## ビルド手順

### 前提条件

- **Rust**: https://rustup.rs/ からインストール
- **Tauri CLI**: `cargo install tauri-cli` (v2)
- **Windows 10 1903以降** (mstsc /l のサポートとWebView2)

### ビルド

```bash
# リポジトリのルートで
cargo tauri build
```

成果物: `src-tauri/target/release/rdp-launcher.exe` (または .msi インストーラーが `src-tauri/target/release/bundle/` に生成)

### 開発モード

```bash
cargo tauri dev
```

### アイコン生成 (初回のみ)

`src-tauri/icons/` に以下が必要:
- `32x32.png`
- `128x128.png`
- `icon.ico`

Tauri CLIで生成可能:
```bash
# 1024x1024 以上の PNG を用意して
cargo tauri icon path/to/source-icon.png
```

または手動で適当なPNGを配置。

## 使い方

### 1. 初回セットアップ

1. アプリを起動
2. 「モニターを検出して始める」をクリック → 接続中のモニターが自動検出される
3. 設定 → 「プロファイル」タブ → プロファイルを作成
   - モニターをクリックで選択/解除
   - ダブルクリックでPrimary指定
4. ホストを追加
   - 「+ ホストを追加」→ .rdpファイルを指定
   - デフォルトプロファイルを選択

### 2. 日常の使い方

- メイン画面でホストカードの「接続」ボタンをクリック
- 別のプロファイルで接続したい場合はドロップダウンで切替
- 再起動後もそのまま動作 (IDは毎回自動解決)

### 3. モニター構成が変わったら

ディスプレイの物理配置 (Windows設定 > ディスプレイ > 配置) を変更した場合:
1. 設定 → モニタータブ → 「自動検出」で再検出
2. プロファイルの確認/調整

## 設定ファイル

`%APPDATA%/rdp-launcher/config.json` に保存。手動編集も可能:

```json
{
  "monitors": {
    "mon-0": { "name": "left 1920x1080", "left": -1920, "top": 0, "width": 1920, "height": 1080 },
    "mon-1": { "name": "center 2560x1440", "left": 0, "top": 0, "width": 2560, "height": 1440 },
    "mon-2": { "name": "right 1920x1080", "left": 2560, "top": 0, "width": 1920, "height": 1080 }
  },
  "profiles": {
    "left-two": {
      "name": "左2画面",
      "monitor_ids": ["mon-0", "mon-1"],
      "primary": "mon-1"
    }
  },
  "hosts": [
    {
      "id": "dev",
      "name": "開発サーバー",
      "rdp_file": "C:\\Users\\me\\dev.rdp",
      "default_profile": "left-two",
      "color": "#5b9aff"
    }
  ]
}
```

## 技術的な補足

### モニターID検出の優先順位

1. **mstsc /l** (最も信頼性が高い): ダイアログを自動キャプチャ → パース → 自動クローズ
2. **Win32 EnumDisplayMonitors** (フォールバック): 列挙順がmstscのIDと一致する前提

### 座標マッチングの仕組み

1. **完全一致**: left, top, width, height が全て一致 → 確定
2. **解像度一致 + 最近傍**: 同解像度のモニターが複数ある場合、座標が最も近いものを選択
3. **不一致**: エラーで停止し再検出を促す

### .rdp の書き換え方針

- **元ファイルは変更しない**: `<元ファイル名>_launch.rdp` という一時コピーを作成
- 書き換える行: `selectedmonitors:s:`, `use multimon:i:`, `screen mode id:i:`
- その他の設定 (認証、帯域、リダイレクトなど) は全てそのまま保持

### セッション検出

- `EnumWindows` で `TscShellContainerClass` クラスのウィンドウを列挙
- ウィンドウタイトル (例: "myhost - Remote Desktop Connection") からホスト名を抽出
- ホスト名の部分一致で接続状態を判定

## トラブルシューティング

**Q: モニターが検出されない**
→ `mstsc /l` をコマンドプロンプトで手動実行して出力を確認。ダイアログが表示されればOK。

**Q: 接続時に「Profile not found」エラー**
→ ホストのデフォルトプロファイルが削除されている。設定で再指定。

**Q: 接続はできるが意図しないモニターに表示される**
→ ディスプレイ配置が変わった可能性。設定 → モニター → 自動検出 で更新。

## ライセンス

MIT
